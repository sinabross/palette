<div class="table-responsive" style="margin-top:100px;">

  <div class="container">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">

      <div class="title-web" style="margin-right:10px;">
        <h3>공지사항</h3>
        <p>업데이트 되는 제품 소식을 확인하세요</p>

      </div>

      <hr style="border: 2px solid #38393b">
      <br>

        <table class="table user-list">
          <thead>
          <tr>
            <th><span>번호</span></th>
            <th><span>제목</span></th>
            <th><span>작성자</span></th>
            <th><span>작성일자</span></th>
          </tr>
          </thead>
          <tbody>
          <% @notices.each do |p| %>
              <tr>
                <td>
                  <p><%=p.id%></p>
                </td>
                <td>
                  <a href="/home/notice_view/<%=p.id%>"><%=p.title%></a>
                </td>
                <td>
                  <%=p.name %>
                </td>
                <td>
                  <%=p.created_at.in_time_zone("Asia/Seoul").strftime("%Y년%m월%d일 %H시%M분%S초") %>
                </td>

              </tr>
          <% end %>
          </tbody>
        </table>

        <%if user_signed_in? && current_user.admin !=nil%>
          <a href="/home/admin_write" style="font-size:13px;color:white;" class="btn btn-black" role="button">글쓰기</a>
         <%end%>
      </div>
  </div>

  <style>
    table a {
      color:#000000;
    }
    table a:hover{

      text-decoration:none;
    }
    .btn-black{
      width:60px;
      height:30px;
      font-size:15px;
      text-align:center;
      float:right;
      cursor:pointer;
      color:#000000;
      background:#333333;
    }
    .title-web h3 {
      font-family: 'NanumGothic';
      font-size: 22px;
      font-weight: bold;
      font-style: normal;
      font-stretch: normal;
      color: #38393b;
    }
    .title-web p{
      font-family: NanumGothic;
      font-size: 14px;
      font-weight: normal;
      font-style: normal;
      font-stretch: normal;
      color: #787878;
    }

  </style>

  <script>
    $(document).ready(function() {

      //글 보기,접기 버튼
        $('.notice-toggle').click(function(){
        //get collapse content selector
        var collapse_content_selector = $(this).attr('href');

        //make the collapse content to be shown or hide
        var toggle_switch = $(this);
        $(collapse_content_selector).toggle(function(){
        if($(this).css('display')=='none'){
        //change the button label to be 'Show'
        toggle_switch.html('보기');
        }else{
        //change the button label to be 'Hide'
        toggle_switch.html('접기');
        }
        });
     });



    });
  </script>